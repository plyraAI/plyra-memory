{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"plyra-memory <p>Persistent structured memory for AI agents.   Local-first. Server-optional. Framework-agnostic.</p> Zero config <p>pip install and go. SQLite + ChromaDB, everything local.</p> Three layers <p>Working, episodic, and semantic memory. One API for all three.</p> Five frameworks <p>LangGraph, AutoGen, LangChain, CrewAI, OpenAI Agents.</p> Server mode <p>Two env vars. All agents share one memory layer.</p>"},{"location":"#install","title":"Install","text":"<pre><code>pip install plyra-memory\n</code></pre>"},{"location":"#the-simplest-possible-usage","title":"The simplest possible usage","text":"<pre><code>from plyra_memory import Memory\n\nasync with Memory() as memory:\n    await memory.remember(\"user prefers Python async frameworks\")\n    ctx = await memory.context_for(\"what stack does the user use?\")\n    print(ctx.content)\n</code></pre> <p>That's it. Memory persists across restarts. No config required.</p>"},{"location":"#how-it-works","title":"How it works","text":"<pre><code>Your agent calls  \u2192  memory.remember(content)\n                         \u2193\n                  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                  \u2502  working memory      \u2502  this session\n                  \u2502  episodic memory     \u2502  all sessions, vector search\n                  \u2502  semantic memory     \u2502  extracted facts, decay model\n                  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                         \u2193\nYour agent calls  \u2192  memory.context_for(query)\n                         \u2193\n                  prompt-ready context string, injected before LLM call\n</code></pre>"},{"location":"#next-steps","title":"Next steps","text":"Quickstart <p>Running example in 5 minutes.</p> Concepts <p>How the three memory layers work.</p> Adapters <p>LangGraph, AutoGen, LangChain, CrewAI, OpenAI.</p> Server mode <p>Multi-agent, multi-tenant deployment.</p> <p>Part of the Plyra open-source stack. Also see plyra-guard \u2014 action middleware for agents.</p>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#v020","title":"v0.2.0","text":"<ul> <li>LLM fact extraction (<code>LLMExtractor</code>) \u2014 Anthropic and OpenAI support</li> <li>Auto-promotion: episodes \u2192 semantic facts at access_count\u22653 or age\u22657 days</li> <li>Episodic summarization: LLM-compressed when session exceeds threshold</li> <li><code>Memory.with_anthropic()</code> and <code>Memory.with_openai()</code> convenience constructors</li> <li>Extraction runs as background task \u2014 <code>remember()</code> is non-blocking</li> <li><code>RegexExtractor</code> extracted into proper class, now the default fallback</li> <li>123 tests passing</li> </ul>"},{"location":"changelog/#v010","title":"v0.1.0","text":"<ul> <li>Three-layer cognitive memory: working, episodic, semantic</li> <li>Local storage: SQLite + ChromaDB + sentence-transformers</li> <li>Universal API: <code>memory.remember()</code> + <code>memory.context_for()</code></li> <li>Framework adapters: LangGraph, AutoGen, LangChain, CrewAI, OpenAI Agents</li> <li>HTTP server: <code>plyra-memory serve</code> \u2192 localhost:7700</li> <li>CLI: <code>serve</code>, <code>ping</code>, <code>stats</code>, <code>reset</code></li> <li>SemanticCache with configurable similarity threshold</li> <li>94 tests passing on Python 3.11, 3.12, 3.13</li> </ul> <p>GitHub releases</p>"},{"location":"concepts/","title":"Concepts","text":"<p>How plyra-memory's three-layer cognitive model works.</p>"},{"location":"concepts/#the-three-layers","title":"The three layers","text":"<pre><code>                    memory.remember(content)\n                           \u2193\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 Working memory                               \u2502\n\u2502 Current session. Max 50 entries.             \u2502\n\u2502 Lowest-importance evicted when full.         \u2502\n\u2502 Flushed to episodic on session end.          \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 Episodic memory                              \u2502\n\u2502 Permanent event log. Every session.          \u2502\n\u2502 Vector-embedded. Retrieved by similarity.    \u2502\n\u2502 Auto-promoted to semantic at access_count\u22653  \u2502\n\u2502 or age\u22657 days.                               \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 Semantic memory                              \u2502\n\u2502 Structured facts. Subject/predicate/object.  \u2502\n\u2502 Confidence decay over time.                  \u2502\n\u2502 Fingerprint deduplication.                   \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                           \u2193\n                  memory.context_for(query)\n                    \u2192 prompt-ready string\n</code></pre>"},{"location":"concepts/#working-memory","title":"Working memory","text":"<p>Working memory holds the current conversation \u2014 raw messages and tool outputs from this session. It's fast, session-scoped, and bounded.</p> <ul> <li>Capacity: 50 entries (configurable via <code>working_max_entries</code>)</li> <li>Eviction: when full, the lowest-importance entry is deleted</li> <li>Lifetime: current session only</li> <li>Flush: when the session ends (or <code>memory.flush()</code> is called),   working entries are converted to episodic events</li> </ul> <pre><code># Working memory \u2014 happens automatically inside remember()\nfrom plyra_memory import Memory\n\nmemory = Memory()\nasync def test():\n    await memory.working.add(dict(\n        session_id=memory.session_id,\n        content=\"user asked about deployment\",\n        importance=0.7,\n        source=\"user_message\",\n    ))\n</code></pre>"},{"location":"concepts/#episodic-memory","title":"Episodic memory","text":"<p>Episodic memory is the permanent event log. Every exchange is stored and never deleted (unless summarized). It's vector-embedded so retrieval is semantic \u2014 not keyword matching.</p> <ul> <li>Storage: SQLite (facts) + ChromaDB (vectors)</li> <li>Retrieval: cosine similarity on sentence-transformer embeddings</li> <li>Lifetime: forever (or until summarized when session exceeds threshold)</li> <li>Auto-promotion: episodes accessed \u22653 times or older than 7 days   are automatically promoted to semantic facts</li> </ul> <pre><code># Episodic \u2014 happens automatically inside remember()\nfrom plyra_memory import Memory\nfrom plyra_memory.schema import EpisodeEvent, Episode\n\nmemory = Memory()\nasync def test2():\n    await memory.episodic.record(Episode(\n        session_id=memory.session_id,\n        agent_id=memory.agent_id,\n        event=EpisodeEvent.USER_MESSAGE,\n        content=\"user asked about LangGraph deployment\",\n        importance=0.8,\n    ))\n</code></pre>"},{"location":"concepts/#semantic-memory","title":"Semantic memory","text":"<p>Semantic memory stores structured facts \u2014 things the agent knows about the world or the user that should persist regardless of how they came up.</p> <ul> <li>Structure: subject \u2192 predicate \u2192 object (e.g. <code>user PREFERS Python</code>)</li> <li>Deduplication: fingerprint on (agent_id, subject, predicate) \u2014   same fact updated in place, not duplicated</li> <li>Decay: confidence decays exponentially over time:   <code>score = confidence \u00d7 exp(-0.05 \u00d7 days_since_access)</code></li> <li>Predicates: IS, PREFERS, DISLIKES, USES, WORKS_ON, BELONGS_TO,   LOCATED_IN, KNOWS, HAS, RELATED_TO</li> </ul> <pre><code>from plyra_memory.schema import FactRelation, Fact\n\nmemory = Memory()\nasync def test3():\n    await memory.semantic.learn(Fact(\n        agent_id=memory.agent_id,\n        content=\"user prefers Python\",\n        subject=\"user\",\n        predicate=FactRelation.PREFERS,\n        object=\"Python\",\n        confidence=0.9,\n    ))\n</code></pre>"},{"location":"concepts/#retrieval-and-context-injection","title":"Retrieval and context injection","text":"<p><code>context_for()</code> queries all three layers and assembles a prompt-ready context string within a token budget:</p> <pre><code>from plyra_memory import Memory\n\nmemory = Memory()\nasync def test4():\n    ctx = await memory.context_for(\n        query=\"what does the user prefer?\",\n        token_budget=2048,    # default\n    )\n</code></pre> <p>Retrieval uses hybrid scoring: vector similarity + recency + importance. Semantic facts score highest (structured, high signal). Episodic events score by similarity + how recent they are. Working entries score by importance within the session.</p>"},{"location":"concepts/#auto-promotion","title":"Auto-promotion","text":"<p>Episodes automatically graduate to semantic facts when either trigger fires:</p> <ul> <li>Access count: episode recalled \u22653 times \u2192 promoted</li> <li>Age: episode older than 7 days \u2192 promoted</li> </ul> <p>This means the semantic layer grows organically from conversation history without any explicit <code>semantic.learn()</code> calls.</p> <p>Configure thresholds in MemoryConfig:</p> <pre><code>from plyra_memory import MemoryConfig\n\nconfig = MemoryConfig(\n    semantic_promotion_threshold=3,   # access count trigger\n    promotion_age_days=7,             # age trigger\n    promotion_check_enabled=True,\n)\n</code></pre>"},{"location":"concepts/#episodic-summarization","title":"Episodic summarization","text":"<p>When a session accumulates more than <code>summarize_session_threshold</code> episodes (default 20), plyra-memory compresses them:</p> <ul> <li>Recent episodes (last 30 days): LLM-summarized into one dense episode</li> <li>Old episodes (older than 30 days): deleted entirely</li> </ul> <p>This keeps the database from growing unbounded while preserving signal. Requires an LLM client (<code>Memory.with_anthropic()</code> or <code>Memory.with_openai()</code>).</p> <p>Next: API reference \u2192 or pick your framework \u2192</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>Get persistent memory working in your agent in 5 minutes.</p>"},{"location":"quickstart/#install","title":"Install","text":"<pre><code>pip install plyra-memory\n</code></pre>"},{"location":"quickstart/#basic-usage","title":"Basic usage","text":"<pre><code>import asyncio\nfrom plyra_memory import Memory\n\nasync def main():\n    async with Memory(agent_id=\"my-agent\") as memory:\n\n        # Write to memory\n        await memory.remember(\"my name is Alex\")\n        await memory.remember(\"I prefer Python async frameworks\")\n        await memory.remember(\"I'm building a LangGraph agent\")\n\n        # Read from memory \u2014 prompt-ready context string\n        ctx = await memory.context_for(\"what is the user working on?\")\n        print(ctx.content)\n        # \u2192 \"user is Alex, prefers Python async frameworks,\n        #    building a LangGraph agent\"\n\nasyncio.run(main())\n</code></pre>"},{"location":"quickstart/#cross-session-persistence","title":"Cross-session persistence","text":"<p>Memory persists across restarts automatically. Run this twice \u2014 the second run recalls what the first stored.</p> <pre><code>import asyncio\nfrom plyra_memory import Memory\n\nasync def session():\n    # Same agent_id = same memory across runs\n    async with Memory(agent_id=\"my-agent\") as memory:\n        user_input = input(\"you: \")\n        ctx = await memory.context_for(user_input)\n\n        # Inject context into your LLM prompt\n        prompt = f\"{ctx.content}\\n\\nUser: {user_input}\" if ctx.content else user_input\n\n        # ... call your LLM here ...\n        response = \"I remember you from last time!\"\n\n        # Store the exchange\n        await memory.remember(user_input, source=\"user_message\")\n        await memory.remember(response, source=\"agent_response\")\n        print(f\"agent: {response}\")\n\nasyncio.run(session())\n</code></pre>"},{"location":"quickstart/#with-llm-fact-extraction","title":"With LLM fact extraction","text":"<p>Pass an Anthropic or OpenAI client to extract structured facts automatically:</p> <pre><code>import anthropic\nfrom plyra_memory import Memory\n\nclient = anthropic.Anthropic()\nmemory = Memory.with_anthropic(api_key=client.api_key, agent_id=\"my-agent\")\n\nasync with memory:\n    # \"my name is Alex\" \u2192 fact: user IS Alex (confidence 0.95)\n    # \"I prefer TypeScript\" \u2192 fact: user PREFERS TypeScript\n    # \"I'm building a SaaS\" \u2192 fact: user WORKS_ON SaaS\n    await memory.remember(\"my name is Alex, I prefer TypeScript\")\n</code></pre> <p>See LLM fact extraction for full details.</p>"},{"location":"quickstart/#with-a-framework","title":"With a framework","text":"LangGraphAutoGenLangChainCrewAIOpenAI Agents <pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.langgraph import create_memory_nodes\nfrom langgraph.graph import StateGraph\n\n# Needs dummy values to parse correctly for quickstart stub\nState = dict\nyour_llm_node = lambda state: state\n\nmemory = Memory(agent_id=\"my-agent\")\nctx_node, rec_node = create_memory_nodes(memory)\n\ngraph = StateGraph(State)\ngraph.add_node(\"memory_in\",  ctx_node)   # reads memory before LLM\ngraph.add_node(\"llm\",        your_llm_node)\ngraph.add_node(\"memory_out\", rec_node)   # writes memory after LLM\ngraph.add_edge(\"memory_in\",  \"llm\")\ngraph.add_edge(\"llm\",        \"memory_out\")\n</code></pre> <pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.autogen import MemoryHook\n\nmemory = Memory(agent_id=\"my-agent\")\nhook = MemoryHook(memory)\n\n# agent.register_hook(\"process_all_messages_before_reply\", hook.before_reply)\n# agent.register_hook(\"process_message_before_send\",       hook.before_send)\n</code></pre> <pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.langchain import PlyraMemory\n# from langchain.chains import ConversationChain\n\nmemory = Memory(agent_id=\"my-agent\")\n# chain = ConversationChain(llm=your_llm, memory=PlyraMemory(memory))\n</code></pre> <pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.crewai import MemoryTool\n# from crewai import Agent\n\nmemory = Memory(agent_id=\"my-agent\")\n# agent = Agent(role=\"Assistant\", tools=[MemoryTool(memory)])\n</code></pre> <pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.openai_agents import create_memory_tools\n\nmemory = Memory(agent_id=\"my-agent\")\ntools = create_memory_tools(memory)\n# Pass tools to your OpenAI function calling setup\n</code></pre>"},{"location":"quickstart/#server-mode","title":"Server mode","text":"<p>Connect to a shared server \u2014 two env vars, no code changes:</p> <pre><code>export PLYRA_SERVER_URL=http://localhost:7700\nexport PLYRA_API_KEY=plm_live_abc123...\n</code></pre> <pre><code># Identical code \u2014 routes to server automatically\nasync def test():\n    async with Memory(agent_id=\"my-agent\") as memory:\n        await memory.remember(\"user prefers Python\")\n</code></pre> <p>See server mode for full setup.</p>"},{"location":"quickstart/#whats-stored-where","title":"What's stored where","text":"Layer What How long Working Current session messages Session only Episodic All exchanges, all sessions Forever, vector-indexed Semantic Extracted facts (name, prefs, project) Forever, decay model <p>\u2192 Concepts explains each layer in depth.</p> <p>Next: Concepts \u2192 or jump to your framework adapter \u2192</p>"},{"location":"adapters/","title":"Adapters","text":"<p>plyra-memory works with every major agent framework through thin adapter wrappers. All adapters expose the same two operations:</p> <pre><code>context_for(query)   \u2192   read memory before LLM call\nremember(content)    \u2192   write memory after agent responds\n</code></pre>"},{"location":"adapters/#framework-support","title":"Framework support","text":"Framework Import Pattern LangGraph <code>create_memory_nodes(memory)</code> Two nodes in StateGraph AutoGen <code>MemoryHook(memory)</code> Register on ConversableAgent LangChain <code>PlyraMemory(memory)</code> Drop-in for ConversationBufferMemory CrewAI <code>MemoryTool(memory)</code> Tool in Agent tools list OpenAI Agents <code>create_memory_tools(memory)</code> Function tools Plain Python <code>Memory()</code> directly No adapter needed"},{"location":"adapters/#key-design-principle","title":"Key design principle","text":"<p>No framework is required at import time.</p> <pre><code># This works even if langgraph is not installed\nfrom plyra_memory.adapters.langgraph import create_memory_nodes\n</code></pre> <p>Framework imports happen lazily inside methods, not at module level. <code>pip install plyra-memory</code> pulls in zero framework dependencies.</p>"},{"location":"adapters/#server-mode","title":"Server mode","text":"<p>All adapters work identically in server mode. Set <code>PLYRA_SERVER_URL</code> + <code>PLYRA_API_KEY</code> \u2014 no adapter code changes needed.</p> <pre><code>export PLYRA_SERVER_URL=http://localhost:7700\nexport PLYRA_API_KEY=plm_live_abc123\n</code></pre> <p>\u2192 Server mode docs</p> <p>Pick your framework: LangGraph \u2192 \u00b7 AutoGen \u2192 \u00b7 LangChain \u2192 \u00b7 CrewAI \u2192 \u00b7 OpenAI Agents \u2192 \u00b7 Plain Python \u2192</p>"},{"location":"adapters/autogen/","title":"AutoGen","text":"<pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.autogen import MemoryHook\n\nmemory = Memory(agent_id=\"my-agent\")\nhook = MemoryHook(memory)\n\n# Register on agent manually using corresponding hook functionality.\n</code></pre>"},{"location":"adapters/crewai/","title":"CrewAI","text":"<pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.crewai import MemoryTool\n\nmemory = Memory(agent_id=\"my-agent\")\ntool = MemoryTool(memory)\n</code></pre>"},{"location":"adapters/langchain/","title":"LangChain","text":"<pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.langchain import PlyraMemory\n\nmemory = Memory(agent_id=\"my-agent\")\nlc_memory = PlyraMemory(memory)\n</code></pre>"},{"location":"adapters/langgraph/","title":"LangGraph","text":"<p>Two patterns: two nodes (recommended) or one combined node.</p>"},{"location":"adapters/langgraph/#install","title":"Install","text":"<pre><code>pip install plyra-memory langgraph\n</code></pre>"},{"location":"adapters/langgraph/#two-node-pattern-recommended","title":"Two-node pattern (recommended)","text":"<pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.langgraph import create_memory_nodes\n# from langgraph.graph import StateGraph, MessagesState, END\n\nmemory = Memory(agent_id=\"my-agent\")\nctx_node, rec_node = create_memory_nodes(memory)\n\n# graph = StateGraph(MessagesState)\n# graph.add_node(\"memory_in\",  ctx_node)     # reads before LLM\n# graph.add_node(\"llm\",        your_llm_node)\n# graph.add_node(\"memory_out\", rec_node)     # writes after LLM\n\n# graph.set_entry_point(\"memory_in\")\n# graph.add_edge(\"memory_in\",  \"llm\")\n# graph.add_edge(\"llm\",        \"memory_out\")\n# graph.add_edge(\"memory_out\", END)\n</code></pre> <p><code>ctx_node</code> injects memory context into <code>state[\"memory_context\"]</code>. Use it in your LLM node:</p> <pre><code># async def your_llm_node(state: MessagesState) -&gt; dict:\n#     memory_ctx = state.get(\"memory_context\", \"\")\n#     messages = state[\"messages\"]\n\n#     system = f\"You are a helpful assistant.\\n\\n{memory_ctx}\" if memory_ctx else \\\n#              \"You are a helpful assistant.\"\n\n#     response = await llm.ainvoke([SystemMessage(system)] + messages)\n#     return {\"messages\": [response]}\n</code></pre>"},{"location":"adapters/langgraph/#single-node-pattern","title":"Single-node pattern","text":"<p>For simpler graphs where one node handles both read and write:</p> <pre><code># from plyra_memory.adapters.langgraph import memory_node\n# graph.add_node(\"memory\", memory_node(memory))\n</code></pre> <p>LangGraph ToolNode</p> <p>Do not use <code>guard.wrap()</code> or direct memory wrapping with <code>ToolNode</code>. LangGraph's <code>ToolNode</code> manages internal state that conflicts with middleware. Always use <code>create_memory_nodes()</code> or <code>memory_node()</code> as graph nodes.</p>"},{"location":"adapters/langgraph/#state-schema","title":"State schema","text":"<p><code>ctx_node</code> writes to <code>state[\"memory_context\"]</code> (str). Declare it in your state class:</p> <pre><code># from typing import Annotated\n# from langgraph.graph import MessagesState\n\n# class State(MessagesState):\n#     memory_context: str = \"\"\n</code></pre> <p>\u2190 Adapters overview \u00b7 AutoGen \u2192</p>"},{"location":"adapters/openai-agents/","title":"OpenAI Agents","text":"<pre><code>from plyra_memory import Memory\nfrom plyra_memory.adapters.openai_agents import create_memory_tools\n\nmemory = Memory(agent_id=\"my-agent\")\ntools = create_memory_tools(memory)\n</code></pre>"},{"location":"adapters/plain-python/","title":"Plain Python","text":"<pre><code>import asyncio\nfrom plyra_memory import Memory\n\nasync def main():\n    async with Memory() as memory:\n        await memory.remember(\"I am testing\")\n</code></pre>"},{"location":"api/","title":"API Reference","text":"<p>Complete reference for the <code>Memory</code> class and all public methods.</p>"},{"location":"api/#memory-class","title":"Memory class","text":"<pre><code>from plyra_memory import Memory, MemoryConfig\n\n# Zero config \u2014 local SQLite + ChromaDB\nmemory = Memory()\n\n# With agent identifier\nmemory = Memory(agent_id=\"support-agent\")\n\n# With LLM fact extraction (Anthropic)\n# memory = Memory.with_anthropic(api_key=\"sk-ant-...\")\n\n# With LLM fact extraction (OpenAI)\n# memory = Memory.with_openai(api_key=\"sk-...\")\n\n# Full config\n# memory = Memory(\n#     config=MemoryConfig(...),\n#     agent_id=\"my-agent\",\n#     extractor=LLMExtractor(client),\n#     llm_client=client,\n# )\n</code></pre>"},{"location":"api/#constructor-parameters","title":"Constructor parameters","text":"Parameter Type Default Description <code>config</code> <code>MemoryConfig \\| None</code> <code>None</code> Full config object. Uses defaults if omitted. <code>agent_id</code> <code>str</code> <code>\"default-agent\"</code> Stable identifier for this agent. Use same value across restarts to retain memory. <code>session_id</code> <code>str \\| None</code> <code>None</code> Current session ID. Auto-generated if omitted. <code>extractor</code> <code>BaseExtractor \\| None</code> <code>None</code> Fact extractor. Uses <code>RegexExtractor</code> fallback if omitted. <code>llm_client</code> <code>Any \\| None</code> <code>None</code> LLM client for episodic summarization."},{"location":"api/#class-methods","title":"Class methods","text":"Method Description <code>Memory.with_anthropic(api_key, ...)</code> Convenience constructor with Anthropic client <code>Memory.with_openai(api_key, ...)</code> Convenience constructor with OpenAI client"},{"location":"api/#instance-methods","title":"Instance methods","text":"Method Returns Description <code>await memory.remember(content, ...)</code> <code>dict</code> Write to all memory layers <code>await memory.context_for(query, ...)</code> <code>ContextResult</code> Get prompt-ready context <code>await memory.recall(query, ...)</code> <code>RecallResult</code> Search memory <code>await memory.flush()</code> <code>list[Episode]</code> Flush working memory to episodic <code>await memory.close()</code> <code>None</code> Close connections, cancel tasks"},{"location":"api/#properties","title":"Properties","text":"Property Type Description <code>memory.agent_id</code> <code>str</code> Agent identifier <code>memory.session_id</code> <code>str</code> Current session ID <code>memory.working</code> <code>WorkingMemoryLayer</code> Direct working layer access <code>memory.episodic</code> <code>EpisodicLayer</code> Direct episodic layer access <code>memory.semantic</code> <code>SemanticLayer</code> Direct semantic layer access"},{"location":"api/#context-manager","title":"Context manager","text":"<pre><code>from plyra_memory import Memory\n\nasync def test():\n    # Recommended \u2014 handles close() automatically\n    async with Memory(agent_id=\"my-agent\") as memory:\n        await memory.remember(\"hello\")\n\n    # Or manually\n    memory = Memory(agent_id=\"my-agent\")\n    try:\n        await memory.remember(\"hello\")\n    finally:\n        await memory.close()\n</code></pre>"},{"location":"api/#server-mode","title":"Server mode","text":"<p>When <code>PLYRA_SERVER_URL</code> and <code>PLYRA_API_KEY</code> are set, <code>Memory()</code> routes all operations to the remote server automatically.</p> <pre><code>export PLYRA_SERVER_URL=http://localhost:7700\nexport PLYRA_API_KEY=plm_live_abc123\n</code></pre> <pre><code>from plyra_memory import Memory\n\n# Same code \u2014 local or server\nasync def test():\n    async with Memory(agent_id=\"my-agent\") as memory:\n        await memory.remember(\"hello\")\n</code></pre> <p>\u2192 Server mode docs</p> <p>Methods: remember() \u2192 \u00b7 context_for() \u2192 \u00b7 recall() \u2192 \u00b7 Layer access \u2192 \u00b7 MemoryConfig \u2192 \u00b7 Schema \u2192</p>"},{"location":"api/config/","title":"MemoryConfig","text":"<p>Full configuration reference for plyra-memory.</p>"},{"location":"api/config/#import","title":"Import","text":"<pre><code>from plyra_memory import MemoryConfig\n</code></pre>"},{"location":"api/config/#all-options","title":"All options","text":"<pre><code>from plyra_memory import MemoryConfig\n\nconfig = MemoryConfig(\n    # Storage\n    store_url=\"~/.plyra/memory.db\",        # SQLite path\n    vectors_url=\"~/.plyra/memory.index\",   # ChromaDB path\n    embed_model=\"all-MiniLM-L6-v2\",        # sentence-transformers model\n\n    # Working memory\n    working_max_entries=50,                # max entries per session\n\n    # Retrieval\n    default_token_budget=2048,             # default for context_for()\n    fusion_weights={                       # must sum to 1.0\n        \"semantic\": 0.5,\n        \"episodic\": 0.3,\n        \"working\":  0.2,\n    },\n\n    # SemanticCache\n    cache_enabled=True,\n    cache_similarity_threshold=0.90,\n\n    # Auto-promotion\n    semantic_promotion_threshold=3,        # access_count trigger\n    promotion_age_days=7,                  # age trigger\n    promotion_check_enabled=True,\n\n    # Episodic summarization\n    summarize_enabled=True,\n    summarize_session_threshold=20,        # episodes per session before summarizing\n    summarize_recent_days=30,              # recent = within N days \u2192 LLM summary\n    summarize_max_episodes=10,             # max episodes per summary call\n\n    # Server mode (auto-detected from env vars)\n    # Set via: PLYRA_SERVER_URL + PLYRA_API_KEY\n)\n</code></pre>"},{"location":"api/config/#environment-variables","title":"Environment variables","text":"<p>All config fields can be set via env vars with <code>PLYRA_</code> prefix:</p> <pre><code>PLYRA_STORE_URL=~/.plyra/memory.db\nPLYRA_WORKING_MAX_ENTRIES=50\nPLYRA_CACHE_ENABLED=true\nPLYRA_SEMANTIC_PROMOTION_THRESHOLD=3\n</code></pre>"},{"location":"api/config/#per-use-case-presets","title":"Per-use-case presets","text":"<pre><code>from plyra_memory import MemoryConfig\n\n# Minimal \u2014 fastest, no summarization\nconfig = MemoryConfig(\n    cache_enabled=False,\n    summarize_enabled=False,\n    promotion_check_enabled=False,\n    working_max_entries=20,\n)\n\n# Full intelligence \u2014 needs LLM client\nconfig = MemoryConfig(\n    summarize_enabled=True,\n    summarize_session_threshold=15,\n    promotion_check_enabled=True,\n    semantic_promotion_threshold=2,\n)\n\n# High-traffic server\nconfig = MemoryConfig(\n    cache_enabled=True,\n    cache_similarity_threshold=0.85,   # more aggressive caching\n    summarize_session_threshold=10,    # summarize sooner\n    working_max_entries=30,\n)\n</code></pre> <p>\u2190 Layer access \u00b7 Schema \u2192</p>"},{"location":"api/context-for/","title":"memory.context_for()","text":"<p>Retrieve relevant memory and return a prompt-ready context string. Call this before every LLM invocation.</p>"},{"location":"api/context-for/#signature","title":"Signature","text":"<pre><code># await memory.context_for(\n#     query: str,\n#     token_budget: int = 2048,\n#     layers: list[MemoryLayer] | None = None,\n# ) -&gt; ContextResult\n</code></pre>"},{"location":"api/context-for/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>query</code> <code>str</code> required What to search for. Usually the user's latest message. <code>token_budget</code> <code>int</code> <code>2048</code> Max tokens in returned context. Memories added until budget exhausted. <code>layers</code> <code>list[MemoryLayer] \\| None</code> <code>None</code> Which layers to search. Defaults to all three."},{"location":"api/context-for/#returns-contextresult","title":"Returns: ContextResult","text":"<pre><code># ctx.content        # str  \u2014 inject this into your LLM prompt\n# ctx.token_count    # int  \u2014 tokens used\n# ctx.token_budget   # int  \u2014 budget requested\n# ctx.memories_used  # int  \u2014 number of memories included\n# ctx.cache_hit      # bool \u2014 True if result served from SemanticCache\n# ctx.latency_ms     # float\n</code></pre>"},{"location":"api/context-for/#usage","title":"Usage","text":"<pre><code>from plyra_memory import Memory\n\nmemory = Memory()\nasync def test():\n    ctx = await memory.context_for(\"what stack does the user prefer?\")\n    user_message = \"hey\"\n    # Inject into prompt\n    prompt = f\"You are a helpful assistant.\\n\\n{ctx.content}\\n\\nUser: {user_message}\"\n</code></pre>"},{"location":"api/context-for/#retrieval-scoring","title":"Retrieval scoring","text":"<p>Memories are scored and ranked before selection:</p> <pre><code>semantic facts      weight: 0.5   (highest \u2014 structured, high signal)\nepisodic events     weight: 0.3   (vector similarity + recency)\nworking entries     weight: 0.2   (importance score, current session)\n\nfinal_score = semantic_score \u00d7 0.5\n            + episodic_score \u00d7 0.3\n            + working_score  \u00d7 0.2\n</code></pre> <p>Memories are added to context in score order until <code>token_budget</code> is reached.</p>"},{"location":"api/context-for/#limit-to-specific-layers","title":"Limit to specific layers","text":"<pre><code>from plyra_memory.schema import MemoryLayer\nfrom plyra_memory import Memory\n\nmemory = Memory()\nquery = \"what\"\nasync def test():\n    # Semantic only \u2014 fastest, structured facts only\n    ctx = await memory.context_for(\n        query,\n        layers=[MemoryLayer.SEMANTIC],\n    )\n\n    # Episodic + semantic \u2014 skip working memory\n    ctx = await memory.context_for(\n        query,\n        layers=[MemoryLayer.EPISODIC, MemoryLayer.SEMANTIC],\n    )\n</code></pre>"},{"location":"api/context-for/#semanticcache","title":"SemanticCache","text":"<p><code>context_for()</code> results are cached by query similarity. If a new query is within 0.90 cosine similarity of a cached query, the cached result is returned immediately.</p> <p>Configure:</p> <pre><code>from plyra_memory import MemoryConfig\n\nconfig = MemoryConfig(\n    cache_enabled=True,                    # default True\n    cache_similarity_threshold=0.90,       # default\n)\n</code></pre> <p>\u2190 remember() \u00b7 recall() \u2192</p>"},{"location":"api/layers/","title":"Layer Access","text":"<p>Direct low-level access to the memory layer APIs.</p>"},{"location":"api/recall/","title":"<code>memory.recall()</code>","text":"<p>The internal recall mechanism retrieving memory manually.</p>"},{"location":"api/remember/","title":"memory.remember()","text":"<p>Universal write method. One call persists content to all relevant memory layers.</p>"},{"location":"api/remember/#signature","title":"Signature","text":"<pre><code># await memory.remember(\n#     content: str,\n#     importance: float = 0.6,\n#     source: str | None = None,\n#     metadata: dict | None = None,\n# ) -&gt; dict\n</code></pre>"},{"location":"api/remember/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>content</code> <code>str</code> required The content to store <code>importance</code> <code>float</code> <code>0.6</code> Priority 0.0\u20131.0. Higher = less likely to be evicted from working memory. <code>source</code> <code>str \\| None</code> <code>None</code> Content origin. Maps to EpisodeEvent. <code>metadata</code> <code>dict \\| None</code> <code>None</code> Arbitrary key-value metadata stored with the entry."},{"location":"api/remember/#source-values","title":"Source values","text":"Value EpisodeEvent <code>\"user_message\"</code> <code>USER_MESSAGE</code> <code>\"agent_response\"</code> <code>AGENT_RESPONSE</code> <code>\"tool_output\"</code> <code>TOOL_RESULT</code> <code>\"tool_call\"</code> <code>TOOL_CALL</code> <code>None</code> or <code>\"agent\"</code> <code>AGENT_RESPONSE</code>"},{"location":"api/remember/#returns","title":"Returns","text":"<pre><code>{\n    \"working_entry\": \"WorkingEntry | None\",\n    \"episode\": \"Episode | None\",\n    \"facts\": [],   # always [] \u2014 extraction runs in background\n}\n</code></pre> <p>Fact extraction is asynchronous</p> <p>Facts appear in semantic memory within ~500ms after <code>remember()</code> returns. Use <code>memory.semantic.query()</code> to check after a short delay if needed.</p>"},{"location":"api/remember/#what-it-does-internally","title":"What it does internally","text":"<pre><code>remember(content)\n    \u2193\n    \u251c\u2500\u2500 working.add()           synchronous \u2014 available immediately\n    \u251c\u2500\u2500 episodic.record()       synchronous \u2014 available immediately\n    \u2514\u2500\u2500 asyncio.create_task()   background \u2014 fact extraction via extractor\n              \u2193\n        RegexExtractor (default) or LLMExtractor\n              \u2193\n        semantic.learn() for each detected fact\n</code></pre>"},{"location":"api/remember/#examples","title":"Examples","text":"<pre><code>from plyra_memory import Memory\n\nmemory = Memory()\nasync def test():\n    # Basic\n    await memory.remember(\"user asked about deployment\")\n\n    # With source and importance\n    await memory.remember(\n        \"user said they prefer TypeScript\",\n        source=\"user_message\",\n        importance=0.85,\n    )\n\n    # With metadata\n    await memory.remember(\n        \"tool returned 42 results\",\n        source=\"tool_output\",\n        metadata={\"tool\": \"web_search\", \"query\": \"python async\"},\n    )\n</code></pre>"},{"location":"api/remember/#fact-extraction-patterns","title":"Fact extraction patterns","text":"<p>When using <code>RegexExtractor</code> (default), these patterns trigger fact storage:</p> Pattern Predicate Example <code>my name is X</code> IS <code>my name is Alex</code> <code>I prefer X</code> PREFERS <code>I prefer async Python</code> <code>I don't like X</code> DISLIKES <code>I don't like verbose APIs</code> <code>I use X</code> USES <code>I use VSCode</code> <code>I'm working on X</code> WORKS_ON <code>I'm working on a RAG pipeline</code> <code>I work at X</code> BELONGS_TO <code>I work at Acme Corp</code> <code>I'm based in X</code> LOCATED_IN <code>I'm based in Chennai</code> <code>I know X</code> KNOWS <code>I know LangChain well</code> <p>For higher recall, use LLMExtractor.</p> <p>\u2190 API overview \u00b7 context_for() \u2192</p>"},{"location":"api/schema/","title":"Pydantic Schema","text":"<p>Pydantic schema to deal with events, working structures. See schema file for all details.</p>"},{"location":"extraction/","title":"Fact Extraction","text":"<p>How plyra-memory extracts structured facts from free text.</p>"},{"location":"extraction/#overview","title":"Overview","text":"<p>When you call <code>memory.remember(\"my name is Alex\")</code>, plyra-memory automatically detects this as a fact and stores it in semantic memory:</p> <pre><code>subject:    user\npredicate:  IS\nobject:     Alex\nconfidence: 0.95\n</code></pre> <p>This happens in the background \u2014 <code>remember()</code> returns immediately.</p>"},{"location":"extraction/#two-extractors","title":"Two extractors","text":"Extractor Speed Recall Cost RegexExtractor instant ~60% free LLMExtractor ~500ms ~95% API call per message"},{"location":"extraction/#default-behavior","title":"Default behavior","text":"<p>Without configuration, <code>RegexExtractor</code> is used:</p> <pre><code>from plyra_memory import Memory\n\nmemory = Memory()\nasync def test():\n    # Uses regex \u2014 fast, no API calls\n    await memory.remember(\"I prefer TypeScript\")\n    # \u2192 fact: user PREFERS TypeScript\n</code></pre>"},{"location":"extraction/#with-llm-extraction","title":"With LLM extraction","text":"<pre><code>import anthropic\nfrom plyra_memory import Memory\n\n# memory = Memory.with_anthropic(api_key=\"sk-ant-...\")\n# await memory.remember(\"I mostly reach for TypeScript these days\")\n# \u2192 fact: user PREFERS TypeScript   \u2190 caught by LLM, missed by regex\n</code></pre>"},{"location":"extraction/#custom-extractor","title":"Custom extractor","text":"<p>Implement <code>BaseExtractor</code> to use any extraction method:</p> <pre><code>from plyra_memory.extraction.base import BaseExtractor\nfrom plyra_memory.schema import FactRelation\nfrom plyra_memory import Memory\n\nclass MyExtractor(BaseExtractor):\n    async def extract(self, text: str, agent_id: str) -&gt; list[dict]:\n        # your logic\n        return [{\"subject\": \"user\", \"predicate\": FactRelation.KNOWS,\n                 \"object\": \"something\", \"confidence\": 0.8}]\n\nmemory = Memory(extractor=MyExtractor())\n</code></pre> <p>\u2192 Custom extractor guide</p> <p>RegexExtractor \u2192 \u00b7 LLMExtractor \u2192 \u00b7 Custom \u2192</p>"},{"location":"extraction/custom/","title":"Custom Extractions","text":"<p>Custom integrations of extraction hooks to the memory structure.</p>"},{"location":"extraction/llm/","title":"LLM Extractor","text":"<p>Extraction via Anthropic and OpenAI LLM models seamlessly.</p>"},{"location":"extraction/regex/","title":"Regex Extractor","text":"<p>Extraction via Regular Expression patterns across memory statements.</p>"},{"location":"guides/cross-session/","title":"Cross-session Persistence","text":"<p>Keeps all state using UUIDs natively resolving your session requirements across restarts.</p>"},{"location":"guides/fact-extraction/","title":"Fact Extraction Tips","text":"<p>Adjust constraints such as recency, and decay metrics seamlessly.</p>"},{"location":"guides/local-vs-server/","title":"Local vs Server Mode","text":"<p>How to decide which mode to use.</p>"},{"location":"guides/local-vs-server/#decision-guide","title":"Decision guide","text":"<pre><code>Are you building a prototype or running locally?\n  \u2192 Local mode. Zero setup.\n\nDo you need memory shared across multiple agents or machines?\n  \u2192 Server mode.\n\nIs this a production deployment with multiple users?\n  \u2192 Server mode with workspace isolation.\n\nAre you unsure?\n  \u2192 Start local. Add PLYRA_SERVER_URL later \u2014 zero code changes.\n</code></pre>"},{"location":"guides/local-vs-server/#comparison","title":"Comparison","text":"Local Server Install <code>pip install plyra-memory</code> <code>docker compose up</code> Config none two env vars Storage <code>~/.plyra/</code> mounted volume Agents one process unlimited, any machine Isolation by <code>agent_id</code> workspace \u2192 user \u2192 agent Scale single node multi-instance with Postgres LLM extraction optional optional (configured server-side) Cost free hosting cost"},{"location":"guides/local-vs-server/#migration-local-server","title":"Migration: local \u2192 server","text":"<p>No code changes required. Just add env vars:</p> <pre><code># Was: local mode (no env vars needed)\n# Now: server mode\nexport PLYRA_SERVER_URL=https://your-server.azurecontainerapps.io\nexport PLYRA_API_KEY=plm_live_abc123\n</code></pre> <p>Your agent code stays identical.</p> <p>\u2190 Server overview \u00b7 Multi-agent \u2192</p>"},{"location":"guides/multi-agent/","title":"Multi-agent Deployment","text":"<p>Combine Agents and let them share their environments through context API features.</p>"},{"location":"guides/production/","title":"Production Checklist","text":"<p>Checks to run continuously to achieve production ready quality.</p>"},{"location":"server/","title":"Server Mode","text":"<p>Connect plyra-memory to a shared server \u2014 all agents share one memory layer.</p>"},{"location":"server/#when-to-use-server-mode","title":"When to use server mode","text":"Local Server Agents one process unlimited, any machine Memory <code>~/.plyra/</code> mounted volume or Postgres Isolation by <code>agent_id</code> workspace \u2192 user \u2192 agent Setup zero two env vars Use case development, single-agent production, multi-agent teams"},{"location":"server/#how-it-works","title":"How it works","text":"<pre><code>Agent A  \u2500\u2510\nAgent B  \u2500\u2500\u2524\u2500\u2500 plyra-memory-server \u2500\u2500 persistent storage\nAgent C  \u2500\u2518         \u2191\n               plm_live_abc123\n</code></pre> <p><code>Memory()</code> detects <code>PLYRA_SERVER_URL</code> and routes all operations to the server via HTTP. No code changes required.</p>"},{"location":"server/#quickstart","title":"Quickstart","text":"<pre><code># 1. Run the server\ndocker run -p 7700:7700 \\\n  -e PLYRA_ADMIN_API_KEY=your-admin-key \\\n  ghcr.io/plyraai/plyra-memory-server:latest\n\n# 2. Create an API key\ncurl -X POST http://localhost:7700/admin/keys \\\n  -H \"Authorization: Bearer your-admin-key\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"workspace_id\": \"my-workspace\", \"env\": \"live\"}'\n\n# 3. Set env vars\nexport PLYRA_SERVER_URL=http://localhost:7700\nexport PLYRA_API_KEY=plm_live_abc123...\n</code></pre> <pre><code># Same code as local mode\nfrom plyra_memory import Memory\n\nasync def test():\n    async with Memory(agent_id=\"my-agent\") as memory:\n        await memory.remember(\"user prefers Python\")\n        ctx = await memory.context_for(\"what stack?\")\n</code></pre>"},{"location":"server/#workspace-isolation","title":"Workspace isolation","text":"<p>Every API key belongs to a workspace. Memory is isolated between workspaces.</p> <pre><code># Workspace \"acme\" \u2014 key plm_live_acme...\n# Workspace \"other\" \u2014 key plm_live_other...\n# They cannot see each other's memory \u2014 enforced server-side\n</code></pre> <p>Within a workspace, memory is further namespaced by <code>user_id</code> and <code>agent_id</code>.</p> <p>\u2192 Full server docs \u2192 Azure deployment \u2192 Connect to server quickstart</p> <p>\u2190 Extraction \u00b7 Guides \u2192</p>"},{"location":"server/azure/","title":"Azure Container Apps Deployment","text":"<p>Deploy plyra-memory-server to Azure Container Apps. Free tier available. Scales to zero when idle.</p>"},{"location":"server/azure/#prerequisites","title":"Prerequisites","text":"<pre><code># Install Azure CLI\ncurl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash\naz login\naz extension add --name containerapp --upgrade\n</code></pre>"},{"location":"server/azure/#deploy","title":"Deploy","text":"<pre><code># 1. Create resource group\naz group create --name plyra-rg --location eastus\n\n# 2. Create Container Apps environment\naz containerapp env create \\\n  --name plyra-env \\\n  --resource-group plyra-rg \\\n  --location eastus\n\n# 3. Create Azure File Share for persistent storage\naz storage account create \\\n  --name plyrastorage \\\n  --resource-group plyra-rg \\\n  --sku Standard_LRS\n\naz storage share create \\\n  --name plyra-data \\\n  --account-name plyrastorage\n\n# 4. Deploy the container\naz containerapp create \\\n  --name plyra-memory-server \\\n  --resource-group plyra-rg \\\n  --environment plyra-env \\\n  --image ghcr.io/plyraai/plyra-memory-server:latest \\\n  --target-port 7700 \\\n  --ingress external \\\n  --min-replicas 0 \\\n  --max-replicas 3 \\\n  --env-vars \\\n    PLYRA_ADMIN_API_KEY=secretref:admin-key \\\n    PLYRA_STORE_URL=/data/memory.db \\\n    PLYRA_VECTORS_URL=/data/memory.index \\\n    PLYRA_KEY_STORE_URL=/data/keys.db\n</code></pre>"},{"location":"server/azure/#get-your-url","title":"Get your URL","text":"<pre><code>az containerapp show \\\n  --name plyra-memory-server \\\n  --resource-group plyra-rg \\\n  --query properties.configuration.ingress.fqdn \\\n  --output tsv\n# \u2192 plyra-memory-server.eastus.azurecontainerapps.io\n</code></pre>"},{"location":"server/azure/#connect-the-library","title":"Connect the library","text":"<pre><code>export PLYRA_SERVER_URL=https://plyra-memory-server.eastus.azurecontainerapps.io\nexport PLYRA_API_KEY=plm_live_...\n</code></pre> <pre><code>from plyra_memory import Memory\n\nasync def test():\n    async with Memory(agent_id=\"my-agent\") as memory:\n        await memory.remember(\"hello from Azure\")\n</code></pre> <p>\u2190 Server overview \u00b7 Guides \u2192</p>"},{"location":"server/quickstart/","title":"Quickstart (Server)","text":"<p>Startup the server easily locally.</p>"}]}